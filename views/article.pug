html
    head
        title Blog

    body
        p donner votre avis
        form(method="POST" )
            textarea(id="desc", name="desc", placeholder="Some great article content...")
            <input class="btn btn-info btn-lg" type="submit" value="usefull">

        h1 #{game.title}
        h2 #{game.desc}
        h2 #{game.ratio}

        p La liste des articles
        for comment in comments
            h1 #{comment.userName}
            h2 #{comment.desc}